import java.util.HashMap;
import java.util.Scanner;


public class Main {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        StoreControl controller = new StoreControl();
        controller.runStore(scanner);
        scanner.close();
    }
}
